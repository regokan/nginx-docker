version: "3"
services:
  web1:
    build:
      context: ./express-app
      dockerfile: .
    environment:
      - SERVER_NAME=1
  web2:
    build:
      context: ./express-app
      dockerfile: .
    environment:
      - SERVER_NAME=2
  web3:
    build:
      context: ./express-app
      dockerfile: .
    environment:
      - SERVER_NAME=3
  web4:
    build:
      context: ./express-app
      dockerfile: .
    environment:
      - SERVER_NAME=4
  nginx:
    restart: always
    build:
      context: ./nginx
      dockerfile: .
    ports:
      - "80:80"
